# Super_ADB v2026.09.22 更新说明

## 修复（Bug Fixes）

### 1. 设备重启失败误报
- **问题**：自研模式下点"重启设备"，reboot 后连接正常断开，却被误报为"官方 adb server 侧 offline"错误。
- **修复**：`重启设备()` 方法加 try-except 包裹，忽略 reboot 导致的连接断开异常。

### 2. 日志抓取卡死
- **问题**：自研 adb shell 流意外退出（设备断开、app 崩溃等）时，日志查看器一直卡在"抓取中"状态，没有任何提示。
- **修复**：
  - 自研 adb 客户端 `shell流()` 新增 `on_exit` 回调参数
  - 日志查看器新增 `_shell_exited` 信号 + `_on_shell_unexpected_exit` 槽函数
  - shell 流意外退出时自动停止抓取并提示用户

### 3. 切换模式卡死（Windows 未响应）
- **问题**：环境配置切换 adb 模式时，"关闭所有 client + 清空缓存"操作在主线程执行，阻塞 UI 导致 Windows 弹出"未响应"对话框。
- **修复**：`刷新设置()` 中关闭 client 操作移到后台 daemon 线程，不阻塞主线程。

### 4. 自动连接不显示设备名
- **问题**：开启"启动时自动连接 ADB 设备"，打开软件后设备下拉框只显示 IP:端口，不显示设备型号。
- **根因**：局域网扫描只验证端口开放（CNXN 握手），没有建立完整 ADB 连接（AUTH 握手），所以拿不到 `getprop ro.product.model`。
- **修复**：
  - 新增 `异步获取设备型号()` 方法，后台线程建立连接 + 获取型号
  - UI 层 QTimer 轮询（0.8 秒间隔），拿到型号后即时更新下拉框文本
  - 超时保护：最多轮询 15 次（约 12 秒）

### 5. 第一个设备要等所有设备连完才显示
- **问题**：第一个设备早就连接成功了，但下拉框要等后面的设备（包括连接超时的）都处理完才一起刷新。
- **根因**：之前把"建立连接 + 获取型号"放在 `获取设备列表()` 里**同步执行**，要全部跑完才返回结果。
- **修复**：
  - `获取设备列表()` 改为快速返回（只扫描，型号从缓存取）
  - 型号改为异步获取，拿到一个更新一个，不等所有设备都连完

### 6. 自动连接干等 60 秒授权超时
- **问题**：扫描到非 ADB 设备（或未授权设备）后，自动去做完整 AUTH 握手，干等 60 秒才超时失败。
- **根因**：`验证ADB设备` 只要对方回了 `AUTH` 就认为"是 ADB 设备"，但 AUTH 意味着需要授权弹窗——自动连接阶段不应该去做完整握手。
- **修复**：
  - `验证ADB设备` 升级，返回 `(is_adb, is_authorized)` 双值
  - 未授权设备标记 `_需授权`，仍然显示在下拉框里，但**不自动去连接**
  - 用户手动点"连接"时再走完整授权流程

---

## 优化（Improvements）

### 1. JSON 树视图交互优化
- 自定义 `drawBranches` 绘制 **+ / -** 圆角方块图标
- 重写 `mousePressEvent` 实现 **单击** 展开/折叠（之前要双击）
- 新增 **全部展开 / 全部折叠** 按钮
- 默认只展开第一层，避免初始界面太挤

### 2. 设备列表响应速度优化
- 设备列表**快速返回**：扫描到设备立即显示，不等所有设备都获取完型号
- 型号**异步补全**：后台逐个获取，拿到一个更新一个，用户不用干等
- 未授权设备**自动跳过**：不在自动连接阶段浪费时间等授权

---

## 平台同步

以上所有修改均同步到三个平台：
- **Windows** (`Super_ADB_Win/`)
- **macOS** (`Super_ADB_MAC/`)
- **Linux** (`Super_ADB_Linux/`)

---

## 涉及文件

| 文件 | 修改内容 |
|------|---------|
| `工具/android调试工具/ADB工具.py` | 重启设备容错、刷新设置移后台、获取设备列表快速返回、新增异步获取型号、扫描阶段标记未授权 |
| `工具/android调试工具/自研adb/自研adb客户端.py` | shell流 新增 on_exit 回调 |
| `工具/android调试工具/自研adb/adb协议.py` | 验证ADB设备 返回 (is_adb, is_authorized) |
| `项目启动入口/主入口_设备管理.py` | 异步获取型号 + QTimer 轮询更新、未授权设备跳过自动连接 |
| `页面/日志查看器页面.py` | shell 流意外退出时自动停止抓取 |
| `对话框/JSON工具对话框.py` | 树视图 +/- 图标、单击展开折叠、全部展开/折叠按钮 |
